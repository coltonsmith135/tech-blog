{{#each posts as |post| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <div class="text-center">
  <h2>{{user.userName}}</h2>
  <p>{{description}}</p>
  <p class="created">Created by {{user.userName}} on {{format_date date_created}}</p>
  
</div>   
</div>
</div>
{{/each}}